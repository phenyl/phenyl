// @flow
import type { Id } from './id'
export interface Session {
  id: Id,
  ttl: number,
  entityName: string,
  userId: Id,
}


export interface PreSession {
  id?: Id,
  ttl: number,
  entityName: string,
  userId: Id,
}
