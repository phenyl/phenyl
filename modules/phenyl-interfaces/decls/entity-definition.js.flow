// @flow

import type { Session } from './session.js.flow'
import type { ClientPool } from './client.js.flow'
import type { RequestData } from './request-data.js.flow'
import type { ResponseData } from './response-data.js.flow'
import type { ExecutionWrapper } from './handler.js.flow'

export interface EntityDefinition {
  acl(reqData: RequestData, session: ?Session, clients: ClientPool): Promise<boolean>, // AclHandler
  validation(reqData: RequestData, session: ?Session, clients: ClientPool): Promise<boolean>, // ValidationHandler
  executionWrapper(reqData: RequestData, session: ?Session, clients: ClientPool, execution: ExecutionWrapper): Promise<ResponseData>, // ExecutionWrapper
}

export type EntityDefinitions = { [entityName: string]: EntityDefinition }
