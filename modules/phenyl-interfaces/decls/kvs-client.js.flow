// @flow

import type { Id } from './id.js.flow'

export interface KvsClient<T, PreT> {
  get(id: ?Id): Promise<?T>,
  create(value: PreT): Promise<T>,
  set(value: T): Promise<T>,
  delete(id: ?Id): Promise<boolean>,
}
