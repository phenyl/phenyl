{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["./build/**", "./lib/**"],
      "dependsOn": ["^build"]
    },
    "test": {},
    "lint": {
      "outputs": [".eslintcache"]
    },
    "clean": {
      "cache": false
    },
    "type-check": {}
  }
}
